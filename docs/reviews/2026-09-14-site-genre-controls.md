# サイト表示での YP4G ジャンル制御部分の除去

2026-09-14。ユーザー報告の `sp@@ - 真・女神転生Ⅴ Vengeance` から `sp@@` を除外する表示修正。

## 資料と確認範囲

ローカル checkout を参照。以下の作業ツリーはすべて clean。GitHub main の最新との差分は未確認。

| リポジトリ | コミット | 根拠 |
| --- | --- | --- |
| peca-docs | `820ca526127643e987c4d91d1a8d2ec0551ce63d` | `docs/yp/genre.md` 全文。`{YPプレフィックス}[{ネームスペース}:][?][@...]ジャンル文字列` |
| peercast-0yp | `80325f2b3f8534412d351f5547d6cc23132a8771` | `docs/yp/genre.md` 全文、`internal/httpd/indextxt.go#genreDisplay`。yp と制御部分を除いて一覧を出力 |
| pcgw | `8cab31088104089f0649eef908594e570f26fbce` | `models/genre.rb#initialize`。`.p` 接頭辞、任意ネームスペース・?・@ 列を分離 |

`?` は人数非表示、`@` はポートチェック、`@@` はビットレート基準の帯域チェック、`@@@` は 2Mbps 固定のチェック指定。ジャンル本文ではない。

## 修正と差異

- `ui/src/site-channel-text.ts#channelExplanation` で表示用ジャンルだけを整形。一覧・詳細・検索に共通適用。
- `sp@@` → 空、`sp@@ゲーム` → `ゲーム`、`ypABC:?@@ゲーム` → `ゲーム`。空のジャンルに区切りは付けない。
- 接頭辞は yp / sp / tp。参照 pcgw の任意 `.p` より限定し、未知の識別子は推測で削除しない。
- 掲載済みの通常ジャンルと PCP の制御付きジャンルが混在するため、`sports` / `space` のような英数字直結で制御記号のない曖昧な文字列は保持する。0yp の無条件の yp 除去とは異なる。日本語本文や明示的な記号のある書式は処理する。
- 先頭だけを処理し、ジャンル本文中の `sp@@` や一般的なコロンは保持。
- カタログ・サイト API・PCP・管理 UI の元データは未変更。人数の秘匿や帯域チェックをサイトで再実装するものではない。

以前の [YP 一覧調査](2026-09-14-yp-directory.md) はカタログが掲載済みジャンルを保持する判断。今回は表示層の追加処理であり、その判断は維持する。

## 検証

- `npm run lint` / `npm run build` 成功。Playwright は desktop / mobile / dark 設定の Chromium で 63 件成功（生成 FLV の再生テストを含む）。

- 表示ヘルパーの表形式テスト：制御のみ、名前空間・フラグ付き、日本語本文、通常の英語ジャンル・コロン・途中の記号、元データを変更しないこと。
- ブラウザー：一覧と詳細で報告された文字列が除去され、説明で検索可能、制御文字は検索対象外になること。
- Go 未変更。実配信との相互接続・外部 YP の掲載動作は今回の検証対象外。

## 2026-09-15: pp 接頭辞の追加

ユーザー報告の `pp? - The Division2 Y8S3` で制御部分が残ったため、表示用の既知接頭辞に `pp` を追加。上記の yp / sp / tp 限定という記録は修正前のもの。

- 再確認: peca-docs `820ca526127643e987c4d91d1a8d2ec0551ce63d` の `docs/yp/genre.md`、pcgw `8cab31088104089f0649eef908594e570f26fbce` の `models/genre.rb#initialize`。両作業ツリーは clean。後者の `.p` は `pp?` も制御付きジャンルとして分離する。
- 修正: `channelExplanation` の接頭辞候補に pp を追加。`pp?` は空、`pp?ゲーム` は `ゲーム`。一般文字列の保護と既存の yp / sp / tp 処理は維持。
- 意図的な差異: pcgw の任意 `.p` とは異なり、未確認の接頭辞や制御記号のない英数字直結文字列は保持する。各公開 YP の最新の掲載規則・実配信データは未確認。
- 検証: 表形式テストに pp のフラグ・名前空間・本文と保持対象を追加。ブラウザの一覧・詳細・検索の再現チェックを報告例の `pp?` / `The Division2 Y8S3` に更新。
- 結果: lint・build 成功。上記の表形式・ブラウザ再現チェックは desktop / mobile / dark の計6件成功。差分チェック成功。Go は変更していないため Go 検証は未実施。
